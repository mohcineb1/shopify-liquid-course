(() => {
  const brief = document.querySelector('[data-runtime-brief]');

  if (!brief) return;

  // Add the browser-owned dispatch-preview behavior here.
  // Keep server-rendered product data and the platform-rule statement intact.
})();
