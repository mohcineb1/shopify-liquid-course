# App and component notes

The reviews app works wherever `.product-main` exists. Horizon variant picker is definitely closed Shadow DOM, so inject a style and observer into it. Every custom element has the same events and app host. No theme version, component API, app target, selector, browser, accessibility, fallback, owner or test evidence is known.
