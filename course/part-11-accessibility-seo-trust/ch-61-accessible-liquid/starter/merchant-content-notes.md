# Merchant content

Use file names for alt text, let every setting accept blank headings, use whatever colors look best, and treat rich text as already accessible. No locale, content, media, contrast, caption, reviewer or exception data is known.
