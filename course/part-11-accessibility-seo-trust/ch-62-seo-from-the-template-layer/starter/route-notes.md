# Route notes

Block every tag, filter, sort and page-two URL. Canonicalize all product URLs to the homepage. robots always removes URLs from Google. Use the same title and schema on every page. Current routes, domains, locales, search state and owners are unknown.
