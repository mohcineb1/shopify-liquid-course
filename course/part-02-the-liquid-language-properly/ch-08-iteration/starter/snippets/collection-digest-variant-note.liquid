{%- doc -%}
  Renders one presentational note inside the bounded variant preview.

  @param {string} text - The already selected text to display.
{%- enddoc -%}

<span class="collection-digest__variant-note">{{ text | escape }}</span>
