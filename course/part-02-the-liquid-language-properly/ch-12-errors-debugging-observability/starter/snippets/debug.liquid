{%- doc -%}
  Renders a guarded minimal diagnostic payload.
  @param {string} label - Human diagnostic label.
  @param {string} title - Selected collection title.
  @param {number} count - Current visible collection count.
{%- enddoc -%}
{%- comment -%} Render only the declared JSON-shaped diagnostic here. {%- endcomment -%}
