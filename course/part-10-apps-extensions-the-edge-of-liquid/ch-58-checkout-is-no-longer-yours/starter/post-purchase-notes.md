# Post-purchase request — starter notes

Put a warranty upsell, survey, download link, order edit, and all conversion tracking in one confirmation script. It only runs once after payment, so reuse the same code when the buyer checks delivery later. We do not need to know whether the order exists yet or whether a post-purchase extension is enabled.
