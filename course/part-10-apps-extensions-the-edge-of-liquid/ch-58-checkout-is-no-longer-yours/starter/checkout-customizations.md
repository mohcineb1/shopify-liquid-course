# Checkout customizations — starter notes

Keep the old checkout Liquid so the payment method stays hidden. The VIP shipping Script should still run somewhere. The confirmation tracker must fire once, probably from the Thank you page. The cart warning blocks the customer, so no other validation is needed. Nobody knows which plan, app, pixel, report, or checkout configuration is active.
