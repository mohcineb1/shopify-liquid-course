# App placement notes — starter

Put the reviews script everywhere in the layout so it always works. The reviews widget is too tall, so clip it. Chat should be a product app block because it is an app. The app is installed, so it must already be active and permitted in every country.
