# Release record — starter

Deployed banner update sometime today. Preview looked fine.

- Store: Northstar
- Command: `shopify theme push --publish`
- Theme: campaign theme
- Reviewer: marketing

There is no recorded Git commit, remote theme ID/role, preview URL, route, market/account context, approval, or rollback candidate.
