# Theme Check report — starter

- `MissingTemplate`: ignore it globally because this section may be useful later.
- `ParserBlockingScript`: add every check to disabled so the report is green.
- `UnusedAssign`: delete all assigns automatically without inspecting callers.
- `MissingAsset`: create an empty asset with the expected name.

No check versions, file/line context, source documentation, dependency assessment, owner, or re-run evidence is recorded.
