# B2B policy extension proposal

A block-class account extension `[VERIFY]` displays an approved B2B company/location policy. Exact target/API/eligibility is `[VERIFY]`. It uses minimal declared configuration/metafield context, requests no capabilities, provides merchant placement, and omits content safely when scope/configuration is absent. It never mutates payment terms. Protected-data/policy/owner/release facts are `[VERIFY]`.
