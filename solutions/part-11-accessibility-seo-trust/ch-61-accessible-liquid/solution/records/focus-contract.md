# Focus contract

Drawer opener, named initial focus, containment, background behavior, Escape, close action, return target, missing-launcher fallback, rerender/stale-reference policy, editor lifecycle and fixtures are `[VERIFY]`. The shown code demonstrates return-focus ownership, not a universal trap.
