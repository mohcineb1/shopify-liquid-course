# Semantic contract

Cards render as list items with product headings and links; actions use buttons; image alt is reviewed for purpose and is empty only when decorative. Section/item IDs must remain unique in repeated/empty/translated states `[VERIFY]`.
