# Consent-change contract

Test no-choice, decline, accept, later change, API failure, duplicate provider, pixel, data-sale opt-out and vendor-stop behavior. Allowed methods authorize purpose; `currentVisitorConsent` does not. One approved provider and all exact states are `[VERIFY]`.
