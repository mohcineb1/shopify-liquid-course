# Settings contract

Define semantic roles, defaults, consumers, valid values, contrast/responsive requirements, market policy, performance implications and prohibited uses. No arbitrary CSS/HTML or product truth in global settings.
