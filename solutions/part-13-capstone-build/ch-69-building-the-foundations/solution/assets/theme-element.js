class ThemeElement extends HTMLElement { connectedCallback() { if (this.initialized) return; this.initialized = true; this.abortController = new AbortController(); this.onConnect(); } disconnectedCallback() { this.abortController?.abort(); this.initialized = false; } onConnect() {} emit(name, detail = {}) { this.dispatchEvent(new CustomEvent(name, { bubbles: true, detail })); } } window.ThemeElement = ThemeElement;
