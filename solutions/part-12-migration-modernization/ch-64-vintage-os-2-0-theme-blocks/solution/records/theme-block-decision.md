# Theme Block decision

Editorial text is independent and reusable; product-local blocks remain local pending context/heading/form proof. Block targets, limits, app/dynamic sources, editor version and output are `[VERIFY]`.
