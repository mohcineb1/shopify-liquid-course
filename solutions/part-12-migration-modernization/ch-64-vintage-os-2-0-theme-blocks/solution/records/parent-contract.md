# Parent contract

Editorial stack uses `@theme` and `content_for 'blocks'`; it does not retain local blocks. Existing local text needs mapped candidate configuration or merchant action. App support/current behavior is `[VERIFY]`.
