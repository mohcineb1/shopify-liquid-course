# Architecture comparison

Compare actual candidate templates, sections, blocks, snippets, assets, config, merchant state, app hosts, routes, public contracts and owners. Architecture capability is documented; selected version behavior is `[VERIFY]`.
