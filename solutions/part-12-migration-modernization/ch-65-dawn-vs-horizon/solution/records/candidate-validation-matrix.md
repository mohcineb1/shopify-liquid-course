# Candidate validation matrix

Use sanitized fixtures for form/variant, component upgrade/failure, keyboard/errors/no-JS, app host, CSS/custom CSS, editor tile states, locale, performance, update diff, release and rollback. No private DOM or production action.
