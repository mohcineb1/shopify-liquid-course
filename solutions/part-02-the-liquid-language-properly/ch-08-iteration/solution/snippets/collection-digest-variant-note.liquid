{%- doc -%}
  Renders one variant label selected by the caller.

  @param {string} text - The variant title to display.
{%- enddoc -%}

<span class="collection-digest__variant-note">{{ text | escape }}</span>
