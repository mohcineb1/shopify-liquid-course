# Verification record

- Cold load: module is deferred and native form remains rendered.
- Module failure: block its URL; native radio input and submit still work.
- Two roots: each local status changes only inside its form.
- Editor replacement: verify the active lifecycle adapter connects a new root `[VERIFY]`.
- Vendor: block remote host and confirm no product-form failure.
- Sync: verify emitted asset, theme upload, and cache-bypassed network response.
