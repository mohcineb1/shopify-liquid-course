@font-face { font-family: "Recipe Display"; src: url("{{ 'recipe-display-variable.woff2' | asset_url }}") format("woff2"); font-weight: 400 700; font-style: normal; font-display: swap; }
.recipe-heading h2 { font-family: "Recipe Display", var(--font-heading-family); font-weight: var(--recipe-heading-weight); }
