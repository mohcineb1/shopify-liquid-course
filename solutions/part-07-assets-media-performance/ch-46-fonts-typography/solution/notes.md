# Verification record

The recipe display face is not preloaded until measurement proves it is a first-view dependency. `font-display: swap` preserves readable fallback text. Test cold cache, a blocked resource, missing heading bold variant, long translated titles, system-font selection, and recipe weight fallback. For admin-code-editor maintenance, verify whether Admin Files plus `file_url` becomes the correct custom-font ownership path.
