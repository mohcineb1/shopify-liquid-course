# Verification record

| Check | Expected evidence |
| --- | --- |
| Cold load | Minimal base rule protects structure; section asset owns visuals. |
| Two instances | Different root tokens yield independent computed styles. |
| Editor replacement | Fresh root emits one local include and tokens. |
| Duplicate links | Base comes from layout; each section owns one local include. |
| Removal | Removing section changes no sibling styles. |
