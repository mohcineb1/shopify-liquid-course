let activeRequest;
document.addEventListener('change', async (event) => { const changed = event.target.closest('[data-option-value-id]'); if (!changed) return; const surface = changed.closest('[data-product-surface]'); const picker = surface?.querySelector('[data-option-picker]'); if (!surface || !picker) return; activeRequest?.abort(); activeRequest = new AbortController(); const selectedIds = [...picker.querySelectorAll('fieldset input:checked')].map((input) => input.dataset.optionValueId).filter(Boolean); const url = new URL(changed.dataset.productUrl || surface.dataset.productUrl, window.location.origin); url.searchParams.set('section_id', surface.dataset.sectionId); url.searchParams.set('option_values', selectedIds.join(',')); const focusId = changed.id; const status = surface.querySelector('[data-picker-status]'); surface.setAttribute('aria-busy', 'true'); if (status) status.textContent = 'Updating product options…'; try { const response = await fetch(url, {signal: activeRequest.signal}); if (!response.ok) throw new Error('Variant section request failed'); const parsed = new DOMParser().parseFromString(await response.text(), 'text/html'); const nextSurface = parsed.querySelector('[data-product-surface]'); if (!nextSurface) throw new Error('Variant section missing from response'); surface.replaceWith(nextSurface); document.getElementById(focusId)?.focus(); const nextStatus = nextSurface.querySelector('[data-picker-status]'); if (nextStatus) nextStatus.textContent = nextSurface.querySelector('input[name="id"]') ? 'Product options updated.' : 'This option combination is unavailable.'; } catch (error) { if (error.name !== 'AbortError' && status) status.textContent = 'Product options could not be updated. Try again.'; } finally { surface.setAttribute('aria-busy', 'false'); } });
